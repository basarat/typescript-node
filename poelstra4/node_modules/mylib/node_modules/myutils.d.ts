// Typing for myutils v1.0
// Note: it's a typing as Typescript would generate (it did, actually): no `declare module "..." { }`, no `/// <reference>`

export interface SomeOldInterface {
    foo: string;
}
export declare function something(): SomeOldInterface;
