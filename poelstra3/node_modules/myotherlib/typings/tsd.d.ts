/// <reference path="../typings/node/node.d.ts" />
/// <reference path="../typings/bluebird/bluebird.d.ts" />

// Note: this file is currently necessary to pick up 'wrapped' typings (`declare module "..." { }`), typically:
// - typings found on e.g. DefinitelyTyped
// - node.d.ts

// Note: don't include the 'new style' typings of `myutils`!

// Would be cool if inclusion of these typings could e.g. be moved to `tsconfig.json`:
// {
//     typings: ["./typings/bluebird/bluebird.d.ts", "./typings/node/node.d.ts"]
// }
