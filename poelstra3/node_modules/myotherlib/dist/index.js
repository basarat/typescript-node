/// <reference path="../typings/tsd.d.ts" />
// Note: this <reference> path is necessary to pick up 'wrapped' typings:
// - stuff found on e.g. DefinitelyTyped
// - node.d.ts
// Shouldn't be needed if suggestion in ../typings/tsd.d.ts is implemented
var myutils = require("myutils");
var Promise = require("bluebird");
function myotherfunc() {
    return myutils.something();
}
exports.myotherfunc = myotherfunc;
function bleh() {
    return Promise.resolve({
        foo: 42
    });
}
exports.bleh = bleh;
